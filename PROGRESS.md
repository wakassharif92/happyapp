# QA Agent — Build Progress

Tracks status against `qa-agent-spec.md`. Update this as work continues — check items off, move things between sections, add new gaps as they're found.

**Last updated:** 2026-08-31

---

## Status at a glance

| Phase | Status |
|---|---|
| 1. Schema + scaffold + auth | ✅ Done, verified |
| 2. Projects + nav shell | ✅ Done, verified |
| 3. CRUD dashboard pages | ✅ Done, verified |
| 4. Playwright bridge (web) | ✅ Done, verified |
| 5. QA Agent + Automated Testing | ✅ Done, verified |
| 6. Manual triage flow | ✅ Done, verified |
| 7. Programming Agent + fix pipeline | ✅ Done, verified |
| 8. Appium bridge (mobile) | ✅ Done, **verified against real hardware** |
| 9. Polish (realtime, counters, bridge health) | ✅ Done, verified |
| 12. Multi-provider model routing (REQ-100/104–107) | ✅ Verified (DeepSeek + Claude paths); Qwen confirmed at API level, not yet through app |
| 13. Old QA Agent nav collapsed behind a toggle | ⚠️ Superseded by §16 — now commented out entirely, not just collapsed |
| 14. WhatsApp report inbox (REQ-110–115) | ✅ Fully verified end-to-end — real text message and real image, both round-tripped correctly |
| 15. Team report web form + unified inbox (REQ-116–117) | ✅ Fully verified end-to-end — mobile + desktop submission, with/without image and project, WhatsApp path confirmed unaffected |
| 16. Old QA Agent nav commented out + "Other project"/page name fields (REQ-117–119) | ✅ Code verified (`tsc`/`eslint`/`next build` clean); ⚠️ migration `0005` still not confirmed applied to production — traced to the SQL Editor running against a Supabase preview branch instead of main, user was switching branches when work moved on; re-check before relying on `page_name`/`other_project_name` |
| 17. Issue Board prototype at `/dashboard` (REQ-120–124) | ⚠️ Superseded by §19 — Board is now backed by real Supabase tables, not mock data |
| 18. Issue Board is the default landing screen (REQ-125) | ✅ Fully verified — login, root `/`, and the "All Projects" way back all confirmed via a real Playwright run |
| 19. Issue Board real backend + Slack integration (REQ-126–133) | ✅ Fully verified end-to-end — schema applied, `tsc`/`eslint`/`next build` clean, real Playwright + direct-Supabase verification of data persistence and Realtime live-updates; ⚠️ OAuth connect/callback/channel-picker flow only verified for correct graceful-failure behavior — needs a real Slack App + workspace to verify the actual OAuth round-trip (see docs/slack-setup.md) |
| 20. Integrations reachable from the Board's own sidebar; legacy nav decluttered | ✅ Fully verified — real Playwright run confirms the Board's sidebar links straight to the current project's Integrations page, and the old project-scoped sidebar no longer shows Team Reports or a redundant Issue Board link |
| 21. Slack OAuth `redirect_uri` fixed for tunnel-based local dev (REQ-127) | ✅ Fully verified against a real ngrok tunnel + real Slack App — `redirect_uri` sent to Slack's actual `/oauth/v2/authorize` now correctly resolves to the public tunnel URL, not `localhost` |
| 22. Slack channel listing fixed — `missing_scope` on `conversations.list` (REQ-127) | ✅ Fully verified against the real, now-connected IMHealthee workspace — 4 real channels returned correctly |
| 23. Real-time customer support chat + project-scoped team report link (REQ-134–138) | ✅ Fully verified end-to-end in a real browser (both directions, live) + negative RLS test + team report link; `tsc`/`eslint`/`next build` clean |

---

## What's done

### 1. Schema, scaffold, auth
- `supabase/migrations/0001_init.sql` — all 7 tables from Section 3 (REQ-000–006), RLS (`auth.uid() is not null` on everything), `evidence` storage bucket, Realtime enabled on `agent_events`/`test_runs`/`issues`. **Applied to the live Supabase project.**
- `lib/supabase/{client,server,admin}.ts` — browser/server/service-role clients.
- `lib/types/database.ts` — hand-written types (must stay `type`, not `interface` — see note below).
- Email/password auth (`app/login`), `proxy.ts` guards all routes except `/login`.

### 2. Projects + nav shell
- `/projects/new` (REQ-074), project switcher + sidebar nav (REQ-071), `/projects/[id]/settings`.
- "QA It" disabled until `automation_target` + `codebase_path` are set (guard banner in the layout).

### 3. Dashboard CRUD (no AI)
- Modules list + manual add/delete, Report Issue (with Storage screenshot upload), All Issues (filterable table), Approval Queue, Issue detail view.

### 4. Playwright bridge (REQ-092)
- `services/playwright-bridge/server.ts` — Express, one process holds multiple browser contexts keyed by session id.
- `lib/bridge/client.ts` / `lib/bridge/types.ts` — shared contract, also used by the Appium bridge.
- Verified: session create → type/click actions → aria-snapshot DOM → screenshot → close, against a real local page.

### 5. QA Agent + Automated Testing (REQ-010–014, 070, 073)
- `lib/agents/claude.ts` — generic tool-use loop (REQ-100/103, max 25 turns).
- `lib/agents/qa-agent/{generateModules,generateTestCases,runTestSuite}.ts`.
- Live activity feed (`components/ActivityFeed.tsx`, Supabase Realtime) on the module run panel.
- **Verified against real Claude API + Playwright bridge**: synced 2 modules from a requirements doc, generated 16 test cases (happy path + edge cases), ran a scenario that found a genuinely seeded bug (agent double-checked with 3 emails before filing it), and Stop correctly halted after the in-progress case rather than mid-step.

### 6. Manual triage (REQ-050–052)
- `lib/agents/qa-agent/triageIssue.ts`, single + bulk triage API routes, "Needs Triage" queue (`tag=untriaged` filter on All Issues), Approval Queue resolution.
- **Verified**: two seeded issues (one real bug, one matching documented behavior) were correctly classified `bug` / `not_a_bug` with reasoning citing the actual requirement text.
- **Bug caught and fixed during verification**: the triage prompt never told the agent the issue's actual UUID, so it hallucinated one from the title and `update_issue_tag` failed silently. Fixed by (a) including the real UUID in the prompt and (b) hardening the tool to use `ctx.runId` directly during a triage run instead of trusting the model's copy of the id.

### 7. Programming Agent + fix pipeline (REQ-060–063)
- `lib/agents/programming-agent/{tools,runFix,commands}.ts` — `read_file`/`write_file`/`run_command` (allowlisted binaries + git, no shell string parsing — uses `execFile` with argv arrays) / `log_event`, plus `search_codebase` (read-only — added beyond the literal REQ-102 list so the agent can "search further" per REQ-061's own comment).
- `lib/agents/qa-agent/verifyFix.ts` — REQ-063 re-verification.
- **Verified end-to-end on a real git repo**: seeded triaged bug → fix batch trigger → agent read the file, diagnosed the exact root cause, wrote a correct regex fix, ran lint/build, committed with a spec-formatted message referencing the issue id → QA Agent independently re-ran the repro via the browser → issue marked `verified`. Full loop, no human step in the middle.

### 8. Appium bridge (REQ-091) — ✅ verified against real hardware
- `services/appium-bridge/server.ts` — same HTTP contract as the Playwright bridge, via `webdriverio`.
- **Verified end-to-end against a real physical device** (Samsung SM-A155F, connected via USB, visible to `adb devices` — no emulator needed, one was already attached): started a real Appium server (`npx appium --port 4723`, `uiautomator2` driver installed via `npx appium driver install uiautomator2`), started the mobile bridge (`npm run bridge:appium`) pointed at it, created a real session (which launched the Sacrol app — `com.sacrol.app` — on the device), pulled a real screenshot (Sacrol's splash screen) and real page-source/accessibility-tree XML, then cleanly deleted the session. Full round trip confirmed working, not just health-check-level.
- **Fixed along the way**: `JAVA_HOME` wasn't set (only `ANDROID_HOME` was) — Appium needs it explicitly even though a bare `java -version` worked; resolved via `/usr/libexec/java_home`. `.env.local.example`'s `APPIUM_BRIDGE_URL` was dead config — grepped the codebase, confirmed zero references, removed it. `package.json`'s `bridge:appium`/`bridge:playwright` scripts didn't load `.env.local` at all (`tsx` doesn't auto-load dotenv the way Next.js does) — added `--env-file=.env.local` to both so capabilities/ports persist across restarts without manual `export`.
- **Root-caused a real user-reported bug this same session**: Sacrol's `automation_target` was pointing at `http://localhost:4001` — the *Playwright* bridge's port, not an Appium bridge — left over from before Appium was ever set up. Every "QA It" run was failing in ~130ms with `"Failed to start automation session: Error: baseUrl is required"` (the Playwright bridge rejecting a body-less mobile-style request), and since no test case ever started, there was no "running X" progress to show either — not a separate UI bug. Fixed by pointing `automation_target` at the real bridge (`http://localhost:4002`) once it existed.
- **Known gap, unchanged**: desired capabilities (`APPIUM_DEVICE_NAME`/`APPIUM_APP_PACKAGE`/`APPIUM_APP_ACTIVITY`) are still global env vars, not per-project DB fields — fine for Sacrol alone, breaks if a second mobile project needs different capabilities at the same time.

### 9. Polish
- `components/ProgressBar.tsx` on module cards, the run panel, and the dashboard (REQ-073's "progress bars, not just a status string").
- `components/BridgeStatusBadge.tsx` + `checkBridgeHealth()` — proactive "bridge connected / not reachable" indicator on the dashboard (REQ-091).
- Dashboard now shows all 5 counts from REQ-073: open bugs, needs triage, needs approval, fixed (unverified), verified.
- Full regression: clean `tsc --noEmit`, clean `eslint .`, clean `next build`, and a visual walkthrough (light + dark) of every page.

### 10. Performance fixes + light-mode theme redesign (not in the original spec)
- **Nav slowness**: `proxy.ts` switched from `supabase.auth.getUser()` (network round-trip on every request) to `getSession()`; sequential Supabase queries on Dashboard/Testing/Module detail/Issue detail parallelized via `Promise.all`; `BridgeStatusBadge` moved into a `Suspense` boundary with a shorter health-check timeout so it can't block the whole Dashboard render; `loading.tsx` skeleton added to every project sub-route for instant nav feedback.
- **Theme**: dark mode permanently disabled (`@custom-variant dark (&:where(.__qa-agent-dark-disabled))` in `app/globals.css`), new indigo/slate design-token system, `.btn-primary`/`.btn-secondary`/`.btn-danger`/`.card`/`.input` utility classes, applied across every page/component.

### 11. Per-operation API cost tracking (not in the original spec)
- New table `agent_api_calls` (`supabase/migrations/0002_agent_cost_tracking.sql`) — one row per Claude API call (i.e. per turn of `runAgentLoop`), tagged with `project_id`, `operation` (`module_sync` / `test_case_generation` / `test_run` / `issue_triage` / `fix_run` / `verify_fix`), `run_id`, token counts, and computed `cost_usd`. **Applied to the live Supabase project** (user ran it by hand via the SQL Editor, since the CLI couldn't reach this project — see history below).
- `lib/agents/pricing.ts` — per-model $/token pricing (`claude-sonnet-5`'s intro rate vs. standard rate, keyed off the 2026-08-31 cutoff date; other models static).
- `lib/agents/usageTracking.ts` — `recordApiUsage()`, called from an `onUsage(usage, model)` hook on `runAgentLoop` (`lib/agents/claude.ts`) that fires once per turn. `model` is now the actual model that answered that turn (see §12 — this stopped being a fixed constant once model routing shipped). Wired into all 6 agent call sites (`generateModules`, `generateTestCases`, `runTestSuite`, `triageIssue`, `verifyFix`, `runFix`).
- `components/CostSummary.tsx` — renders total cost + a per-operation breakdown (calls, tokens, $) on the project Dashboard.
- **Not yet verified against real usage** — `tsc`/`eslint` are clean and the table now exists in the live project, but no real agent run has been made since this was added, so the actual `agent_api_calls` rows and dashboard numbers haven't been eyeballed yet.

### 12. Multi-provider model routing (REQ-100, 104–107) — ✅ verified end-to-end (DeepSeek path)
`qa-agent-spec.md` was revised to make both agents provider-agnostic instead of Claude-only, with a task→model routing table spanning Claude, DeepSeek, and Qwen (REQ-104), and the QA Agent's screenshot/vision tool removed entirely (REQ-101/092 — this system is now text-only end to end, DOM/accessibility tree only).
- **`lib/agents/providers/`** — `types.ts` (the `ModelProvider` interface + `NormalizedContentBlock`/`ProviderRequest`/`ProviderResponse`, decoupled from `@anthropic-ai/sdk`'s own `ContentBlock`/`Usage` types since those now carry provider-specific required fields like `caller`/`service_tier` that don't exist for other providers), `anthropicProvider.ts` (thin wrapper around the existing Anthropic client), `openaiCompatibleProvider.ts` (one implementation shared by DeepSeek and Qwen — both are OpenAI-compatible chat-completions APIs — via the official `openai` npm package pointed at a custom `baseURL`; does the two-way translation between Anthropic's `Tool`/`MessageParam` shape, which stays the app's one canonical internal format, and OpenAI's request/response shape), `adapter.ts` (`callModel(task, req)` — REQ-105 — resolves the routing table and retries once against the configured fallback if the primary provider call throws).
- **`lib/agents/modelRouting.ts`** (REQ-106) — the task→{provider, model} table, each entry overridable via one env var (`MODEL_ROUTING_<TASK>=provider:model`) without touching any agent code.
- **`lib/agents/claude.ts`**'s `runAgentLoop` now takes a required `task` option and calls `callModel()` instead of the Anthropic SDK directly; `MODEL` (the old fixed constant) is gone — `onUsage(usage, model)` now reports whichever model actually answered each turn, since that varies by task (and, for `fix_run`, can differ from the routing table's primary on a fallback).
- **Two assumptions confirmed with the user** (REQ-104's table doesn't cover them): module sync (REQ-010) routes like test case generation, to DeepSeek V4 Flash. Qwen's exact model id and pricing couldn't be verified — Alibaba's DashScope docs are JS-rendered and unfetchable via WebFetch — so `fix_run` ships with a best-effort default (`qwen3-coder-plus`, international endpoint) and a placeholder cost-tracking rate, both flagged inline in `modelRouting.ts`/`pricing.ts` and correctable via env var / direct edit once confirmed. DeepSeek's model ids and pricing (`deepseek-v4-flash`/`deepseek-v4-pro`) **were** verified live against `api-docs.deepseek.com`.
- **`verifyFix.ts`** doesn't get its own routing entry — REQ-104's "same model as the original test case/issue" is resolved by reusing the `test_run` or `issue_triage` task key based on `issue.source`, rather than inventing a `verify_fix` routing key (the `agent_api_calls.operation` cost-tracking label stays `verify_fix` regardless — separate axis, unaffected).
- Clean `tsc --noEmit`, `eslint .`, and `next build`.
- **`DEEPSEEK_API_KEY` and `QWEN_API_KEY` are now in the real `.env.local`.** Both were smoke-tested with raw `curl` against the real endpoints (not yet through the app's own agent loop):
  - **Qwen — fully confirmed live**: `qwen3-coder-plus` on `https://dashscope-intl.aliyuncs.com/compatible-mode/v1` returned a real `200` completion. The best-effort default guessed during planning was exactly right — no correction needed.
  - **Fixed a real base-URL bug found by this test**: `DEEPSEEK_BASE_URL` defaulted to `https://api.deepseek.com` (no `/v1`, per DeepSeek's own doc wording) but only `https://api.deepseek.com/v1` returns a clean, parseable response — the bare URL gave a bodyless `402`. Corrected in `.env.local`, `.env.local.example`, and the fallback default in `lib/agents/providers/adapter.ts`.
  - `issue_triage` (Claude Sonnet 5) needs no new keys and has been verified in earlier sessions.
- **Full end-to-end verification, real DeepSeek account, through the actual app (not just `curl`)**: after the user funded the DeepSeek account, ran REQ-010's "Sync modules from requirements doc" twice against the real "Sacrol" project via Playwright driving the real UI at `localhost:3000`. Both runs completed successfully (~28s each — `deepseek-v4-flash` is a reasoning model, so a 10-module structured extraction genuinely takes a while) and produced real `agent_api_calls` rows: `model: "deepseek-v4-flash"`, real token counts (e.g. 2317 input / 3266 output), real computed cost (~$0.0012/run). This is hard confirmation that the whole translation path works for real — `openaiCompatibleProvider.ts`'s Tool→function-tool request translation, the `submit_modules` tool call round-trip, JSON-args parsing, `finishTools` capture, and cost logging with the real model name all functioned correctly through a genuine multi-turn-capable loop, not just a raw chat completion.
  - **Found by this same test, not a code bug**: module dedup in `generateModules.ts` matches on exact lowercased name. DeepSeek phrased several module names differently than Claude did in the original sync (e.g. "Playlists Management" vs "Playlist Management" vs "Playlists"; "Insights" vs "Insights (Parent Analytics)"), so the two test runs added 11 near-duplicate modules to the real "Sacrol" project (10 → 21). Confirmed the 11 new ones (by `created_at`) had zero `test_cases`/`issues` attached, then deleted them — "Sacrol" is back to its original 10. The underlying gap (exact-match dedup can't survive model-phrasing drift across providers/runs) is real and still unfixed — flagged in Pending.

### 13. Old QA Agent nav collapsed behind a toggle (not a spec change)
User is starting a new, different project/section in this same app and wanted the original QA Agent nav out of the way without deleting it. `components/OldQaAgentNav.tsx` (new, client component) wraps the brand mark + `NavLinks` + the "not ready" banner behind a collapsed-by-default disclosure toggle labeled "Old QA Agent"; `app/projects/[projectId]/layout.tsx` now renders only `ProjectSwitcher` (always visible) + this toggle in the sidebar by default. Expanding it reveals the full original nav (Dashboard/Automated Testing/Report Issue/All Issues/Approval Queue/Modules/Project Settings) plus a "← All projects" link. Verified visually against the real running app (screenshots of both collapsed and expanded states). Clean `tsc`/`eslint`.

### 14. WhatsApp report inbox (REQ-110–115, `qa-agent-spec.md` Section 11 — new)
The "something else different" §13 made room for. A second, informal issue-intake channel: team members message a WhatsApp Business number directly, messages land in a plain review list — deliberately **not** wired into `issues`/triage, and deliberately **project-agnostic** (confirmed with the user before building).
- **`supabase/migrations/0003_whatsapp_reports.sql`** — `whatsapp_reports` table (`wa_message_id` unique for idempotency against Meta's webhook retries, `sender_name`/`sender_phone`, `message_text`, `image_path`, `category`) + a private `whatsapp-media` Storage bucket, same RLS/signed-URL pattern as the existing `evidence` bucket. **Applied to the live Supabase project** (user ran it via the SQL Editor).
- **`app/api/webhooks/whatsapp/route.ts`** — the one route in the app with no Supabase session (Meta's server calling in, not a browser). `GET` handles the one-time verification handshake; `POST` verifies `X-Hub-Signature-256` (`lib/whatsapp/graphApi.ts`'s `verifyWhatsappSignature`, HMAC-SHA256 over the *raw* body — unit-tested against hand-computed vectors, all pass/reject cases correct), then for each message: text → stored as-is; image → two-step Graph API media download (`downloadWhatsappMedia`) and re-upload to `whatsapp-media`; anything else → stored as `"[unsupported message type: X]"` rather than silently dropped. Inserts via `upsert(..., { onConflict: "wa_message_id", ignoreDuplicates: true })` so Meta's retries can't duplicate rows.
- **`proxy.ts`** — early-return bypass for `/api/webhooks/whatsapp` before the Supabase session flow even starts, since this route authenticates itself via the signature check instead.
- **`app/whatsapp/{layout,page,CategorySelect,actions}.tsx`** — the inbox UI: newest-first list, sender name (falls back to phone), message text, image (signed URL, same pattern as issue evidence), and a Frontend/Backend/Any/— none — dropdown that calls `updateCategory()` directly — plain human action, no AI involved (REQ-113).
- **Nav entry point**: a persistent "WhatsApp Reports" link added to the project sidebar, positioned above the collapsed `OldQaAgentNav` toggle from §13 — new thing up front, old thing tucked away.
- **Verified end-to-end for real, with a real WhatsApp message.** Setup: user created a Meta app ("Ai Agent"), used the free "Try it out" test number, got a temporary access token + phone number ID (both live-verified via a direct Graph API metadata call before proceeding — `200`, real phone number data returned), and a tunnel (`ngrok http 3001`) exposing the local dev server. Verification handshake confirmed both ways (correct token → `200` + challenge echoed; wrong token → `403`) before touching Meta's UI. One real bug caught and fixed live: the webhook POST arrived and parsed correctly, but `whatsapp_reports` didn't exist yet in the live Supabase project (migration not yet applied) — surfaced as a clean `console.error`, not a crash, and Meta doesn't retry once a 200 is returned, so that first test message was unrecoverable (expected — flagged to the user in the moment). After applying `0003_whatsapp_reports.sql`, a second real message ("Hi", from a real WhatsApp contact) round-tripped correctly and rendered on `/whatsapp`.
- **Image path now confirmed too** — a real photo sent over WhatsApp round-tripped correctly: webhook parsed the `image` message type, the two-step Graph API media download worked, the file landed in `whatsapp-media`, and a signed URL served the exact same bytes back out (verified by re-downloading and viewing it, not just checking row/byte counts). The one field shape that was guessed from public docs rather than live-fetched (Meta's image-message reference page 404'd during planning) is now confirmed correct by this real message.
- **The WABA's app-subscription is a separate thing from the webhook field toggles** — worth remembering if this ever needs debugging again: `messages` being "Subscribed" in the app's Webhook Fields list is necessary but not the same check as the WhatsApp Business Account actually having this app in its `subscribed_apps` list (`GET /{waba-id}/subscribed_apps`). Both were confirmed correct here, but they're two independent things that both have to be true.
- **Superseded by §15**: `whatsapp_reports` was renamed to `team_reports` and `app/whatsapp/` moved to `app/reports/` — everything described above is historically accurate (that's genuinely what got built and verified first) but the table/route names it mentions no longer exist under those names.

### 15. Team report web form + unified inbox (REQ-116–117, extends Section 11)
Meta's Business API setup proved too heavy a barrier for routine reporting (real feedback after §14 shipped) — a plain, mobile-responsive, no-login web form is the lower-friction alternative, feeding the same inbox as WhatsApp rather than a competing one.
- **`supabase/migrations/0004_team_reports.sql`** — renamed `whatsapp_reports` → `team_reports` (not a parallel table — a table that holds both channels' reports but is still called `whatsapp_reports` would mislead the next reader), added `source` ('whatsapp' | 'web', existing rows backfilled to `'whatsapp'`) and nullable `project_id`, relaxed `wa_message_id`/`sender_phone` to nullable (web submissions have neither). **Applied to the live Supabase project.** `lib/types/database.ts` updated to match (`WhatsappReport` → `TeamReport`, new `ReportSource` type).
- **`app/team-report/{page,TeamReportForm,actions}.tsx`** — the public form itself: name, project dropdown (fetched via the *admin* client since an anonymous visitor's own client can't read `projects`, RLS requires `auth.uid()`), issue detail, optional image. `proxy.ts` gained a second public-route bypass (alongside the WhatsApp webhook) since requiring login here would defeat the entire point.
- **`app/reports/`** (renamed from `app/whatsapp/`) — now queries `team_reports` across both sources; each row shows a source badge (WhatsApp/Web, new `Badge.tsx` colors) and the project name when one was given. Also now shows the shareable `/team-report` URL at the top of the page so a dev can copy it out to the team. The nav link in the project sidebar (above the collapsed `OldQaAgentNav` toggle) relabeled "Team Reports" → `/reports`.
- **Login page gained a visible "Report an issue" link** to `/team-report` — found via user feedback that nothing in the app actually pointed at the form; this is the one place an unauthenticated visitor is guaranteed to land.
- **Verified end-to-end for real, no mocking**: Playwright driving the actual running app — a mobile-viewport (390×844) submission with an image and a project selected, and a desktop submission with neither, both landed correctly in `team_reports` with `source: 'web'`; the uploaded image round-tripped through Storage correctly (confirmed by re-downloading it, byte-for-byte match). Confirmed the pre-existing WhatsApp row survived the table rename untouched (`source` correctly backfilled to `'whatsapp'`). Screenshots confirmed the form and success state both look right at phone width. Test rows and their uploaded test image were deleted afterward — only real WhatsApp data remains in `team_reports`.

### 16. Old QA Agent nav commented out + "Other project" / page name fields (REQ-117–119, extends §13/15)
User wants the old nav fully out of the way (not just collapsed) to make room for future work, and wants two things devs reviewing `/reports` currently can't get from the web form: which project a report is about when it's not in the dropdown, and which page/screen it happened on.
- **`app/projects/[projectId]/layout.tsx`** — `OldQaAgentNav` import and its entire JSX block are now **commented out** (not deleted), per explicit instruction ("comment the whole QA agent" — keep it trivially restorable). `ProjectSwitcher` and the "Team Reports" link remain active. `isProjectReady` import commented out alongside it since it was only used by the commented block.
- **`supabase/migrations/0005_team_reports_page_name.sql`** (new) — adds `page_name` and `other_project_name` (both nullable text) to `team_reports`. **Not yet applied to the live Supabase project** — SQL handed to the user to run via the SQL Editor, same pattern as every prior migration this session.
- **`lib/types/database.ts`** — `TeamReport` type gained both fields.
- **`app/team-report/TeamReportForm.tsx`** — project `<select>` gained an "Other (not listed)…" option (`__other__` sentinel); choosing it reveals a free-text "Project name" input instead of relying on `project_id`. New optional "Page / screen" text input, always shown.
- **`app/team-report/actions.ts`** — detects the `__other__` sentinel, nulls out `project_id` and stores the typed name in `other_project_name` instead; `page_name` passed straight through.
- **`app/reports/page.tsx`** — inbox rows now show `page_name` (as "Page: …") and fall back to `other_project_name` as a badge when there's no real linked project — otherwise these two new fields would be captured but never visible to the dev reviewing reports, defeating the point of adding them.
- **Why a text fallback instead of letting the form create a real project**: the public form has no session — allowing it to insert into `projects` directly would let anyone with the link create arbitrary projects anonymously. A dev promotes the note to a real project by hand if warranted (see the migration's own comment).
- **Verification status**: `npx tsc --noEmit`, `npx eslint .` (targeted at the changed files), and a full `npx next build` all clean. **Not yet end-to-end verified against a real submission.** Attempted verification hit a real Postgres `42703 column does not exist` error straight from the REST API (not a PostgREST schema-cache staleness issue, as first suspected) — root-caused to the SQL Editor running the migration against a Supabase preview branch rather than the main/production branch that `.env.local`'s API URL actually points at. User was switching to the correct branch to re-run it when work moved on to §17. **Re-run the migration on main and re-verify before trusting `page_name`/`other_project_name` in production.** Not yet re-committed to git.

### 17. Issue Board prototype at `/dashboard` (REQ-120–124, `qa-agent-spec.md` Section 12 — new)
A separate multi-project QA/Issue Tracking board, distinct from both §6's manual triage and §14/15's Team Report Inbox — explicitly requested as a **frontend-only prototype with mock data, no real API calls or Supabase tables**, componentized for easy backend wiring later.
- **`lib/dashboardMock/{types,mockData,relativeTime}.ts`** — mock domain types (6 tabs: In Progress/AI Fix/Pending/Done/Closed/User Complaints; Category; Severity; SourceChannel) and a deterministic (no `Math.random`) mock-data generator producing 5–8 issues per tab across 3 mock projects (Khushoo, IMHealthee, iSpend Wisely) from pools of realistic QA-bug and end-user-complaint seed text.
- **`app/dashboard/theme.css` + `page.tsx`** — a scoped light/dark theme, deliberately **not** using Tailwind's `dark:` variant, since `globals.css` disables that app-wide by design for the rest of the app. Colors are CSS custom properties (`--db-*`, `--status-*`, `--severity-*`) under a `.qa-board` wrapper class with a `[data-theme="dark"]` override block, referenced via Tailwind arbitrary-value utilities (`bg-[var(--db-surface)]`, etc.) — toggling theme here can't leak into any other route.
- **`components/dashboard/`** — `Sidebar`, `TopBar`, `TabNav`, `ProjectSwitcher` (mock-data-driven, separate from the real `components/ProjectSwitcher.tsx`), `IssueCard`, `IssueDetailPanel`, `NewIssueModal`, `CategoryDropdown`, `MoveToMenu`, `StatusBadge`, `SeverityTag`, `Thumbnail`, `EmptyState`, `icons.tsx` (a small hand-written inline-SVG icon set — no icon library is installed in this project, and pulling one in for ~15 glyphs wasn't worth the dependency).
- **State lives entirely in `app/dashboard/page.tsx`'s `useState`** — category changes, tab moves, new comments, and newly created issues all update in-memory and are reflected live in counts/badges, but nothing persists across a reload (matches "mock data" scope).
- **Discoverability**: an "Issue Board" link added next to "Team Reports" in the authenticated project sidebar (`app/projects/[projectId]/layout.tsx`). Sits behind the normal auth guard — no `proxy.ts` bypass, unlike the public team-report form.
- **Verified end-to-end for real**: `tsc`/`eslint`/`next build` all clean. Playwright drove the actual running app through a full real session — created a temporary Supabase test user via the admin API (deleted afterward), logged in, opened `/dashboard`, switched projects, clicked through all 6 tabs, opened the detail panel, toggled dark mode, opened the User Complaints tab (confirmed severity tags + "Convert to Dev Issue" render), submitted the "New Issue" modal (confirmed it lands in the right tab), and checked a 390×844 mobile viewport (sidebar collapses to icon-only, project switcher moves into the top bar, cards stay single-column and readable). One false alarm during verification: an early dark-mode screenshot appeared to show white cards with invisible text — turned out to be `transition-colors` mid-animation at the instant the screenshot fired, not a real bug; a computed-style check and a re-screenshot after the transition settled both confirmed the dark theme applies correctly.

### 18. Issue Board is the default landing screen (REQ-125, extends §17)
User asked to make the Issue Board the project's default screen. Three places controlled where an authenticated user lands, and all three needed the same change (found the third — `app/login/actions.ts` — only because the first Playwright pass exposed it: the URL landed on `/projects` and then auto-forwarded to a specific project despite `proxy.ts` already having been updated):
- **`app/page.tsx`** (`/`) — `redirect("/projects")` → `redirect("/dashboard")`.
- **`proxy.ts`** — the "already signed in, redirect away from `/login`" branch — `/projects` → `/dashboard`.
- **`app/login/actions.ts`** — the sign-in server action's own post-login `redirect("/projects")` → `redirect("/dashboard")`. This one is easy to miss: it fires before `proxy.ts` ever gets a chance to redirect, so fixing only the other two looks right in code review but doesn't actually change where login lands.
- **`components/dashboard/Sidebar.tsx`** — added an "All Projects" link (new `IconFolder` in `icons.tsx`) back to `/projects`, since the old project-scoped area (nav, modules, triage, fix pipeline) is still there and still needed, just no longer the first thing you see.
- **Verified end-to-end for real**: a temporary Supabase test user (created via admin API, deleted after) signing in and landing on `/dashboard`, visiting `/` while authenticated and landing on `/dashboard`, and clicking "All Projects" to confirm the old flow is still one click away.

### 19. Issue Board real backend + Slack integration (REQ-126–133, `qa-agent-spec.md` Section 13 — new)
User asked for Slack integration ("message posted in a connected channel → new Issue in Pending"), but the Issue Board (`/dashboard`) was still 100% mock data at the time — nothing to land a real issue *in*. Flagged this to the user before writing any code; they chose to build a real backend for the Board as part of this work rather than build Slack against the mismatched legacy `issues` table (which is keyed off `module_id` and has no "Pending" status at all — a different pipeline, not a smaller version of the same one).

**Board goes real:**
- **`lib/dashboardMock/` renamed to `lib/board/`** — it's no longer mock, and the old name would have been actively misleading going forward. `types.ts`/`relativeTime.ts` moved as-is; `mockData.ts`'s `MOCK_PROJECTS`/`MOCK_ISSUES` deleted (no longer used), its `initials()` helper moved to a new `format.ts` alongside a new `colorForId()` (deterministic per-issue placeholder color, since real rows have no `thumbnailColor` column). All 14 importing files updated via a scripted path rewrite, confirmed clean by `tsc`.
- **`supabase/migrations/0006_board_issues.sql`** — `board_issues` (project-scoped, `tab`/`category`/`severity`/`source_channel` check-constrained to the exact values the UI uses, `media_url` + `media_type`, `slack_channel_id`/`slack_message_ts` for later dedup), `board_issue_comments`, `board_issue_activity`. Same `authenticated_all` RLS policy shape as every other table in this app. **Applied to production** (after first tripping the same "SQL Editor on a preview branch, not main" issue from a previous session — see below).
- **`lib/types/database.ts`** — added `BoardIssue`/`BoardIssueComment`/`BoardIssueActivity`/`SlackConnection` types, importing the UI's `TabKey`/`Category`/`Severity`/`SourceChannel`/`MediaType` from `lib/board/types.ts` rather than redefining them — caught one real naming collision this way (`Severity` already existed in `database.ts` for the legacy `issues` table with different casing, `"low"` vs `"Low"` — fixed via a `Severity as BoardSeverity` aliased import, not by renaming either domain's existing type).
- **`app/dashboard/page.tsx`** (now a server component) + new **`app/dashboard/DashboardClient.tsx`** (the interactive client half) — same split every other page in this app already uses (e.g. `app/reports/page.tsx` + `CategorySelect.tsx`). `page.tsx` fetches `projects` + `board_issues` once, resolving `media_url` to signed URLs the same way `app/reports/page.tsx` already does for `team_reports.image_path`. `DashboardClient.tsx` keeps local `useState` for snappy interaction, calling server actions (`app/dashboard/actions.ts`: `updateIssueCategory`, `moveIssue`, `addComment`, `getIssueThread`, `createIssue`) to persist; comments/activity are lazy-loaded per issue only when its detail panel opens, not fetched for the whole board up front.
- **`components/dashboard/Thumbnail.tsx`** — extended to render a real `<img>`/`<video>` when `mediaUrl` is present (detail-panel size only; card-row thumbnails stay icon-only to keep the list scannable), falling back to the colored placeholder for issues with no real media (anything created via the "New Issue" modal, still).
- **Realtime**: `DashboardClient.tsx` subscribes to `board_issues` `INSERT` events, same pattern as `ActivityFeed.tsx` (REQ-070) — this is what makes a Slack-created issue appear in an already-open Pending tab without a manual reload.

**Slack integration (REQ-126–133):**
- **`docs/slack-setup.md`** (new) — the Slack App dashboard walkthrough (scopes, OAuth redirect URL, Event Subscriptions URL, where to find each credential) — kept separate from the spec since it's "what to click," not a requirement.
- **`lib/slack/`** (new) — `tokenCrypto.ts` (AES-256-GCM at rest, unit-verified round-trip + tamper-rejection + malformed-input-rejection via a throwaway inline script, same pattern used for the WhatsApp signature check last time — `import "server-only"` throws outside Next's bundler, so these get copy-tested inline rather than imported directly); `verifySignature.ts` (Slack's `v0=` HMAC scheme + 5-minute replay window, verified against 6 hand-computed vectors: valid, tampered body, wrong secret, missing prefix, null headers, stale timestamp — all correct); `oauthState.ts` (signs `project_id` into the OAuth `state` param so a caller can't forge which project a connection attaches to — verified round-trip + tamper + forged-secret rejection); `slackApi.ts` (thin wrapper over `oauth.v2.access`, `conversations.list`, `users.info`, `chat.postMessage`, `auth.revoke`, and the file-download-via-Bearer-token special case); `rateLimit.ts` (in-memory sliding window, 20 events/10s per `team_id` — deliberately not Redis-backed, matching this app's single-long-lived-process architecture, same reasoning as the automation bridges).
- **`app/api/slack/{connect,callback,channels,select-channel,disconnect}/route.ts`** — the OAuth + channel-picker flow exactly as specced. `connect` derives `redirect_uri` from the request's own `Host` header rather than a hardcoded env var, so it can't go stale after a tunnel restart.
- **`app/api/slack/events/route.ts`** — signature-verified, handles `url_verification`, acks real message events with `200` immediately and processes them (user lookup, file download/upload, DB insert, optional thread reply) in a fire-and-forget async call afterward — same "long-lived process, not a real queue" architecture as the automation bridges (REQ-090/115).
- **`proxy.ts`** — bypass added for `/api/slack/events` (Slack's server has no session cookie), same shape as the WhatsApp webhook bypass.
- **`app/projects/[projectId]/integrations/{page,IntegrationsClient}.tsx`** (new) — the Integrations page the spec assumed already existed but didn't; shows connected state (workspace + channel) or a Connect button, opens the channel picker automatically right after a successful OAuth redirect. One real lint fix during this: an effect that read `useSearchParams()` and called `setState` synchronously in the effect body tripped `react-hooks/set-state-in-effect` — fixed by moving the one-time "read the URL" part into a `useState` lazy initializer (a render-time computation, not an effect) and keeping the effect only for the actual side effect (auto-opening the channel picker).
- **Nav**: an "Integrations" link added next to "Team Reports"/"Issue Board" in `app/projects/[projectId]/layout.tsx`.

**A real gap found during verification, not anticipated in the plan**: Supabase Realtime only broadcasts `postgres_changes` for tables explicitly added to the `supabase_realtime` publication — new tables aren't included automatically. `supabase/migrations/0008_board_issues_realtime.sql` (`alter publication supabase_realtime add table board_issues;`) covers this, but **it silently failed to apply twice** before finally taking effect on the third attempt — isolated by writing a standalone Node script that subscribed directly via `@supabase/supabase-js` (bypassing the browser/React layer entirely) and comparing `board_issues` (received nothing) against the already-working `agent_events` (received the test event immediately, identical script) — that comparison is what proved it wasn't an app-side bug. Root cause never fully confirmed (possibly the same preview-branch mismatch as migration `0005` a few sessions ago), but the user directly queried `pg_publication_tables` between attempts and confirmed exactly when the fix actually landed, at which point Realtime worked correctly. **Worth remembering**: `alter publication ... add table` gives no error even when it silently doesn't apply — if a new table's Realtime subscription ever seems to do nothing, verify with `select tablename from pg_publication_tables where pubname = 'supabase_realtime';` rather than assuming the migration ran.

**Verification status**: `tsc --noEmit`, `eslint .` (whole project), and `next build` all clean throughout. Real, no-mocking verification via Playwright + direct Supabase REST calls: logged in as a temporary test user, confirmed the project switcher shows real projects (Sacrol, IMHealthee) not mock ones, created an issue through the "New Issue" modal and confirmed it both rendered in the UI and persisted to `board_issues`, simulated a Slack-webhook-style insert directly via the admin API and confirmed it appeared live in an already-open Pending tab (Realtime path), and confirmed the Integrations page renders the correct "Not connected" state with a working Connect button that redirects to Slack's OAuth flow and fails gracefully with a clear error (expected, since no real `SLACK_CLIENT_ID`/`SLACK_CLIENT_SECRET` are configured yet). **Not yet verified**: the actual OAuth consent-screen round-trip, real channel listing, and a real incoming Slack message — all blocked on a real Slack App existing, which needs the user to work through `docs/slack-setup.md` themselves (requires a Slack workspace and, for local dev, a tunnel). All test users, test issues, and debug rows created during verification were deleted afterward.

### 20. Integrations reachable from the Board's own sidebar; legacy nav decluttered (REQ-124, extends §19)
User feedback right after §19 shipped: "there is no option for me to add slack" — real gap, not a misunderstanding. The Integrations page existed and worked, but was only linked from the *legacy* project-scoped sidebar (`app/projects/[projectId]/layout.tsx`), which the Issue Board's own `Sidebar` never points at — landing on `/dashboard` (the default screen since §18) genuinely had no path to Slack setup without first clicking "All Projects" and navigating into the old area. Same message also asked directly: the old sidebar's "Team Reports"/"Issue Board" links should be hidden, since `/dashboard` being the default landing screen was supposed to mean not seeing that old stuff at all.
- **`components/dashboard/Sidebar.tsx`** — added an "Integrations" link (new `IconPlug` in `icons.tsx`) pointing at `/projects/${currentProjectId}/integrations`, using the Board's own already-selected project — sits above "All Projects" at the bottom of the sidebar.
- **`app/projects/[projectId]/layout.tsx`** — removed the "Team Reports" and self-referential "Issue Board" links entirely (Team Reports itself still works at `/reports`, just isn't linked from here anymore); kept the "Integrations" link as a secondary path for anyone who lands in this legacy area directly, and kept the project switcher since other legacy pages (settings, modules, etc.) still route through this layout.
- **Verified end-to-end for real**: a temporary test user confirmed the Integrations link is visible and clickable directly from the Board's sidebar, that clicking it lands on the correct project's Integrations page without ever visiting `/projects` first, and that the legacy sidebar's content is now just `["<ProjectSwitcher>", "Integrations"]` — no Team Reports, no redundant Issue Board link.

### 21. Slack OAuth `redirect_uri` fixed for tunnel-based local dev (REQ-127, extends §19)
User created a real Slack App and went through `docs/slack-setup.md`, but every Connect attempt failed with Slack's `redirect_uri did not match any configured URIs` — first traced (correctly) to two separate causes on the user's side (missing `SLACK_CLIENT_ID`/`SLACK_CLIENT_SECRET`/`SLACK_SIGNING_SECRET` in `.env.local`, then an ngrok tunnel URL that had changed since it was last registered in the Slack dashboard) — but after fixing both, the exact same error persisted with the *correct* ngrok URL visibly registered in Slack. That pointed at a real code bug, not a config gap.
- **Root cause**: `/api/slack/connect` and `/api/slack/callback` both built `redirect_uri` from `request.nextUrl.origin`. Behind an ngrok tunnel, TLS terminates at ngrok's edge and the tunnel agent forwards to the local Next.js dev server over plain HTTP on `localhost` — so `request.nextUrl.origin` reported `https://localhost:3001` (confirmed via a temporary debug route that echoed request headers, added and removed during diagnosis — the "https" half came from Next.js partially trusting `X-Forwarded-Proto` while still using its own hardcoded dev-server host), not the public ngrok URL actually registered in Slack.
- **`lib/slack/requestOrigin.ts`** (new) — `getPublicOrigin()` reads `X-Forwarded-Proto`/`X-Forwarded-Host` (which the same debug route confirmed ngrok sets correctly) and only falls back to `request.nextUrl.origin` when neither header is present (i.e. no proxy/tunnel in front of the app, the normal production or bare-localhost case). Both `connect` and `callback` now use this consistently — they must agree exactly, since Slack's `oauth.v2.access` token exchange requires the identical `redirect_uri` sent to `/oauth/v2/authorize`. The callback's post-OAuth browser redirect back to the Integrations page was fixed the same way — sending the browser to the local-hop address instead of the tunnel URL would have dropped the session cookie the user had just authenticated with on the public origin.
- **Verified end-to-end for real** against the user's actual Slack App and live ngrok tunnel: logged in via the tunnel's public URL (had to add the `ngrok-skip-browser-warning` header to the Playwright browser context — ngrok's free tier shows a browser-only interstitial that plain `curl` bypasses but a real browser hits), clicked Connect, and decoded the resulting Slack authorize URL's `redirect_uri` param to confirm it now exactly matches the tunnel URL — no code path left un-exercised by this fix.

### 22. Slack channel listing fixed — `missing_scope` on `conversations.list` (REQ-127, extends §19/21)
Immediately after §21's fix, OAuth completed successfully (a real `slack_connections` row landed with `status: 'pending_channel'`, `team_name: 'IMHealthee'`) but the channel picker showed nothing. Diagnosed by decrypting the real stored token (`lib/slack/tokenCrypto.ts`'s logic, copy-tested inline the same way as always) and calling `conversations.list` directly against Slack: `{"ok": false, "error": "missing_scope", "needed": "groups:read", "provided": "channels:read,channels:history,files:read,team:read,chat:write"}`.
- **Root cause**: `lib/slack/slackApi.ts`'s `listChannels()` requested `types=public_channel,private_channel` unconditionally. Slack's `conversations.list` requires the scope for **every** type named in `types`, and fails the whole call if any one is missing — even though the bot had full scope for `public_channel`, asking for `private_channel` too (without the optional `groups:read`/`groups:history` scopes from `docs/slack-setup.md` §2, which this setup correctly didn't add) failed everything, hiding channels the bot could actually see.
- **Fix**: `listChannels()` now requests `types=public_channel` only, matching the documented default bot scopes. Left a comment pointing at `docs/slack-setup.md` for the private-channel path, since re-adding `private_channel` to `types` without also granting the extra scopes would silently reintroduce this exact bug.
- **Verified against the real, connected workspace**: decrypted the real stored token and called `conversations.list` directly — confirmed 4 real channels (`all-imhealthee`, `social`, `new-channel`, `fixes`) now return correctly. `tsc`/`eslint`/`next build` all clean.

### 23. Real-time customer support chat + project-scoped team report link (REQ-134–138, `qa-agent-spec.md` Section 14 — new)
User's ask, translated: each project gets two distinct public links — a **Customer Support** link opening a real-time chat with an external customer (opened from inside the client's own mobile app, which passes the customer's logged-in email along), and an **Internal Team** link for reporting issues, project-scoped this time (unlike the existing global `/team-report`).

**Prerequisite — RLS hardened against anonymous auth (REQ-134):** making the chat real-time requires giving each anonymous customer browser a genuine Supabase Auth identity (`signInAnonymously()`), but anonymous sessions still satisfy `auth.uid() is not null` — the exact check all 13 existing `authenticated_all` policies (audited in full across all 8 prior migrations) plus 4 Storage policies used. `supabase/migrations/0009_harden_rls_against_anonymous_auth.sql` introduces `is_staff()` and redefines all of them. **Applied and staff-access-regression-checked before** anonymous sign-ins were enabled in the Supabase Dashboard — confirmed via a real authenticated Playwright session that nothing broke.
- Enabling anonymous sign-ins itself took two attempts — the dashboard toggle didn't persist the first time (no error shown; confirmed via a direct `POST /auth/v1/signup` returning `anonymous_provider_disabled` even after the toggle visually showed on), fixed by toggling off then back on.

**Schema (REQ-135):** `support_conversations` (`0010_support_chat.sql`) keyed `unique(project_id, customer_email)`, `support_messages`. A new server action `claimConversation()` (`app/support/[projectId]/actions.ts`) re-points `customer_auth_uid` at whichever anonymous session opens the link, keyed by email — so the same customer keeps one conversation across devices. `board_issues.source_channel` gained a `'Team Report'` value.

**Routes:** `/support/[projectId]` (public, blocks with an error if no `?email=` — confirmed with the user rather than assumed), `/report/[projectId]` (public, project-scoped, found during planning that `/projects/[projectId]/report` was already a *different* existing authenticated feature — used a new top-level path instead), `/projects/[projectId]/support` (authenticated agent inbox, linked from the Issue Board's `Sidebar.tsx`). Both links shown with copy buttons on the existing `/projects/[projectId]/integrations` page (`LinksCard.tsx`).

**Two real bugs found and fixed during verification, both significant:**
1. **`support_messages`' customer SELECT policy used a subquery** against `support_conversations`. Realtime reported the channel `"SUBSCRIBED"` but silently never delivered events through it. Fixed in `0011_support_messages_denormalize_customer.sql` by denormalizing `customer_auth_uid` onto every message row (regardless of sender) so the policy becomes a simple equality check — this alone didn't fully fix delivery (see #2), but is independently the correct fix for the subquery-RLS-and-Realtime interaction, which is a documented category of Supabase Realtime unreliability.
2. **The real fix**: `postgres_changes` subscriptions using a server-side `filter:` parameter (e.g. `conversation_id=eq.<id>`) reported `"SUBSCRIBED"` but never delivered INSERT events for either new table — reproduced with the subquery removed, `REPLICA IDENTITY FULL` set on both tables, a production build (ruling out React Strict Mode's dev-only double-invoke), and the sibling `support_conversations` subscription fully disabled (ruling out cross-effect interference). The one thing that worked: `DashboardClient.tsx`'s `board_issues` subscription, which uses **no `filter:` at all** — it subscribes to every INSERT and filters client-side. Switching both new subscriptions to that exact pattern fixed it immediately, confirmed via a minimal debug page that copied `DashboardClient.tsx`'s working snippet verbatim.
3. **A related false alarm mid-diagnosis**: `board_issues` Realtime (proven working earlier this session) *also* stopped delivering via Playwright at one point, while a raw Node script for the same table kept working perfectly. This turned out to be a local Playwright/Chromium sandbox networking issue in this session, not a real regression — confirmed by having the user manually test in their own real browser, which worked correctly in both directions on the first try. **Lesson**: when a previously-verified Realtime path suddenly seems broken via automated browser testing, check whether raw Node (non-browser) still works before assuming the app regressed — a real-browser manual check from the user is more trustworthy than continued automated retries if Playwright itself is suspect.
- **Verified end-to-end for real**: the user manually tested the actual chat in two real browser windows — customer sends, agent sees the conversation appear live and the message on open; agent replies, customer sees it live, both directions confirmed working. A negative RLS test (raw script): a second anonymous customer session could not read the first customer's conversation or messages (0 rows both queries) — the security boundary holds, not just the happy path. The team report link was submitted via Playwright (this part isn't Realtime-dependent, so unaffected by the sandbox issue) and confirmed landing in `board_issues` with the correct `tab`, `source_channel`, and project scoping. All test conversations, messages, and issues created during verification were deleted afterward.

### 24. HappyApp rebrand, Add/Edit Project on the board, unread indicator, image sharing, lightbox, GitHub + Vercel deploy (REQ-139–144, `qa-agent-spec.md` Section 15 — new)
User's ask, translated (Roman Urdu): rename "Issue Board" to HappyApp; add Edit/Add Project options to the board itself; hide the Appium/automation-bridge field; a red-dot unread indicator on the Support nav; image sharing in both Support Chat and the Internal Team report; and an overall professional UI pass. A later message added: full-size image viewer on click.

Also in this round: pushed the repo to `https://github.com/wakassharif92/happyapp.git` and deployed to Vercel production (`https://qa-agent-liart.vercel.app`) at the user's request. Confirmed with the user first (via AskUserQuestion, "Deploy to Vercel anyway") that QA Agent's test-run/Fix pipeline and the automation bridges don't work on Vercel's serverless model — Issue Board, Slack, Support Chat, Team Reports, and Links all do.

**Schema:** `0012_support_conversations_last_read.sql` — `support_conversations.last_read_at`, `support_messages.media_url`/`media_type`.

**One real correctness bug caught before it shipped**: initially hid the Settings form's `automation_target` field by deleting it outright. Caught that `actions.ts`'s `updateProject` does `formData.get("automation_target") as string || null` — an absent field submits `null`, which would have silently wiped any project's existing bridge URL on its next settings save. Fixed with a hidden input carrying the existing value forward instead (`qa-agent-spec.md` REQ-140).

**A second, harder-to-pin bug**: image-attached chat messages sent fine but didn't reliably appear via the Realtime echo-back for the *sender's own* message specifically — text-only messages' echo was reliable, images' wasn't (root cause not fully isolated; possibly the extra async delay from the image upload racing the subscription setup). Rather than keep chasing it, switched both `SupportChatClient.tsx` and `SupportInboxClient.tsx` to append the sender's own message straight from the `.insert().select("*").single()` response (with a local `URL.createObjectURL()` blob for the sender's own instant image preview), so sending never depends on Realtime round-tripping back to yourself. Realtime dedup-by-id still guards the case where the echo does arrive.

**Verification:**
- Round-2 Playwright script (7 checks: settings page loads, automation bridge hidden, add-project option visible, customer sends text, unread dot appears, agent sees message, dot clears after read) — all passed.
- Chat real-time and image sharing in both directions — user manually confirmed in a real browser: "yes i can send, and can see."
- Lightbox (added after the above, on request: "once user click on image, it should be opening full size image viewer") — verified via a scripted Playwright check: seeded a real image message via the admin API against a temp staff user, confirmed the lightbox opens on click, closes on Escape, and closes on backdrop click (not Realtime-dependent, so unaffected by this session's established Realtime/Playwright flakiness). All test data (conversation, uploaded test image, temp staff user) deleted afterward.
- `tsc --noEmit`, `eslint`, and a full `next build` all clean after the round.

**User feedback this round, worth remembering**: asked twice to move faster ("why are you taking too much time?", "please hurry") — during debugging phases especially, prioritize acting over narrating.

### 25. "Send case to devs" ticketing (REQ-145–148, `qa-agent-spec.md` Section 16 — new)
User's ask, translated (Roman Urdu, across two messages): the customer-support agent gets a "Send case to devs" button at the top of their chat with a customer; tapping it opens a modal (email auto-filled, reporter name, detail) that on Send creates an entry in the dev-facing Customer Issues list with a link back to the conversation; devs open that link to see the chat in a modal and can "ask a question" that lands in the *same* chat (as a status-like entry) but is invisible to the customer — the agent relays answers manually; once resolved the agent closes the ticket, the customer sees it's closed and gets a "New Issue" option, and the *next* complaint from the same customer carries its own new ticket number (shown at the top of the agent's chat, referring to whichever ticket is currently active).

**Design decision, not asked for explicitly but load-bearing**: no new "tickets" table — a ticket is just a `board_issues` row (`tab: 'user_complaints'`, `source_channel: 'User Complaint'`, both pre-existing values that had no real intake path wired to them before this). This means the entire existing board (move between tabs, comments, activity log, Realtime) works on tickets with zero new code — "closed" is simply `tab === 'closed'`, and the agent-side "Close ticket" button is just a `board_issues` update under the hood.

**Design decision for the customer-visible "resolved" banner**: the anonymous customer's browser has no RLS access to `board_issues` at all (staff-only), so it can't check ticket status directly. Added `support_conversations.has_open_ticket` as a denormalized flag the customer's own client *can* read, updated by the same two server actions that open/close a ticket. The banner itself is derived, not stored: `!has_open_ticket && any customer-visible 'system' message exists` — since the only customer-visible system message this app ever creates is the "marked resolved" notice, its mere presence is sufficient to know a ticket was resolved at some point, with `has_open_ticket` distinguishing "still resolved" from "a new ticket already reopened it."

**Verification**: `tsc --noEmit`, `eslint`, and a full `next build` all clean. **Not yet functionally verified against the real database** — this round's migration (`0013_support_tickets.sql`) had not been applied by the user as of this write-up; every insert/RLS path is written to the same conventions already proven in Sections 14–15 (client-side `filter:`-free Realtime, `visible_to_customer` gated at the RLS layer rather than just in the UI, session-aware `createClient()` for staff-only actions matching `app/dashboard/actions.ts`), but none of it has been exercised through a real browser session yet. **Before trusting this feature**: apply the migration, then walk the full loop once for real — agent sends a case to devs, dev opens the board and asks a question from the ticket modal, agent sees it inline (customer does not), agent closes the ticket, customer sees the resolved banner and sends a new message, agent sees a fresh "no open ticket" state and can send a second case to devs which gets a new ticket number.

### 25b. Severity on ticket creation, two-way internal reply, and dev-side unread dot (REQ-149, same migration `0013`, still unapplied)
Two follow-up asks in the same round, both still against §25's not-yet-applied migration:
1. **Severity dropdown added to the "Send case to devs" modal** — `createSupportTicket` no longer hardcodes `severity: "Medium"`; the agent picks it (reusing `NewIssueModal.tsx`'s existing `SEVERITIES` dropdown), so devs can see it on `IssueCard`/`IssueDetailPanel` exactly like any other User Complaint (that display was already generic, no changes needed there).
2. **Bidirectional internal side-channel**: added `replyToDevs` (agent → dev) alongside the existing `askDevQuestion` (dev → agent) — deliberately a *separate* small modal (`ReplyToDevsModal.tsx`) rather than a mode toggle on the main chat composer, to eliminate the risk of an agent forgetting which mode they're in and leaking an internal note to the customer (or a real reply getting stuck internal). Required generalizing `ChatWindow.tsx`'s "internal" badge from dev-messages-only to any message with `visible_to_customer === false`, since an agent's own internal reply is otherwise indistinguishable from their normal customer-facing bubble (same `sender_type: 'agent'`).
3. **Dev-side unread dot required a second read-cursor**: the existing `support_conversations.last_read_at` is the *agent's* cursor over the shared message stream; devs needed their own, independent one — added `board_issues.dev_last_read_at`, updated when a dev opens `TicketConversationModal`. `DashboardClient.tsx` batch-fetches the latest internal agent reply per ticket-conversation (same shape as `SupportInboxClient.tsx`'s `latestByConversation`) and keeps it live via a `filter:`-free subscription, showing a red dot on both the `IssueCard` and the "View Conversation" button when it postdates `dev_last_read_at`. The agent-side unread dot (Support nav + per-conversation) already worked off `support_conversations.last_read_at`, so it only needed widening from `sender_type = 'customer'` to `sender_type in ('customer', 'dev')` — no new column needed there.

Still `tsc`/`eslint`/`next build` clean, still unverified against a real database for the same reason as §25.

### 26. Multi-tenant companies + Google Sign-In (qa-agent-spec.md, new section — new, largest change so far)
User's ask, translated (Roman Urdu, across several messages): admins add team members via a generated invite link (no email sent — the admin copies and shares it themselves), each invite carries a name + role, the admin can expire/renew the link (same URL, renew just bumps the expiry), and once claimed the member's name shows up wherever they're the actor on the dashboard. Confirmed via clarifying questions before any schema was written: **full data isolation per company** (not just a label), **invite-only** (the link pre-fills an invite; Google Sign-In is the real credential from claim onward, not the link itself), and **Google Sign-In replaces email/password entirely**, including the account already in production. A second request (CoachPro reference image) asked for a visual redesign, folded into the same plan since several brand-new pages ship as part of this work anyway — not started yet, last in the plan's sequencing.

**Part A + B (schema — applied by the user, migrations 0014/0015):** `companies` + `company_members` (one merged table — an invite IS a not-yet-claimed member row, no separate invites table, matching how support tickets reuse `board_issues` rather than a new table). Every one of the 15 existing tables got its own **denormalized** `company_id` column (not a join through `project_id`) — per the hard lesson from migration 0011 (a correlated-subquery RLS policy silently broke Realtime delivery despite reporting "SUBSCRIBED"), one consistent shape everywhere beats "some tables join, some don't." Backfill created one company for all pre-existing data and made every real (non-anonymous) `auth.users` row an admin/owner of it. Every `authenticated_all`/`staff_all` policy rewritten from `is_staff()` to `is_staff() and company_id = current_user_company_id()`. `team_reports.company_id` stays permanently nullable — a public web submission whose typed project name didn't match a real project has nowhere to derive a company from.

**A real production incident, found and fixed within the same session**: `current_user_company_id()` queries `company_members` to resolve the caller's company — but `company_members`'s own RLS policies call `current_user_company_id()` to evaluate their condition. Every query touching ANY company-scoped table triggered infinite recursion, surfaced as Postgres `54001 stack depth limit exceeded` — reproduced directly with a real login (a temp staff user created via the admin API, added to the backfilled company, signed in via Playwright) before assuming what was wrong. This was live against the shared database Vercel production also points at, so the outage affected both immediately. **Fixed** in `0016_fix_current_user_company_id_recursion.sql`: `SECURITY DEFINER` on the function, so its own internal lookup runs with the function owner's privileges (which bypass RLS on tables they own) instead of re-triggering the caller's policy evaluation — breaks the cycle. **Lesson for any future `current_*_id()`-style helper that queries the very table its own policies gate**: it needs `SECURITY DEFINER` (+ `set search_path`) from the start, or this exact recursion reproduces immediately.

**~20 action/route/agent-pipeline files updated** to thread `company_id` through every insert (found systematically via `tsc` after making the column required — every resulting type error was a real call site that needed it) and to swap the old `user.email` attribution fallback for the team member's actual display name (`getCurrentMember()`, `lib/company.ts`) in the 7 places that used to fall back to email.

**Also fixed while investigating the incident**: there was no way to sign out anywhere in the app — added `signOut()` (`app/login/actions.ts`) and a sign-out option both on the TopBar avatar dropdown and directly on the "No projects yet" empty state (where the user got stuck), since that screen renders before the TopBar does.

**Part C (Google Sign-In — code-complete, external setup pending):** `app/auth/callback/route.ts` handles the OAuth return for all three paths this app needs (fresh sign-in, invite claim, linking) off one shared `exchangeCodeForSession` call, branching on what happens after: invite token present → claim the pending `company_members` row; existing membership → dashboard; neither → `/onboarding` (collects company name / your name / your role, creates the company + first admin member — via the **admin client**, since creating a brand-new company and its first member is a bootstrap operation that doesn't fit the normal RLS model requiring an *existing* admin of the target company). `docs/google-oauth-setup.md` walks through the external Google Cloud Console + Supabase Dashboard configuration this needs before any of it can actually run.

**Staged rollout completed for real**: existing account linked its Google identity via the temporary `/link-google` page, confirmed signing back in via Google landed in the same account with the same data — at that point the password form, `authenticate`/`linkGoogleAccount` actions, and `app/link-google/` were all deleted. `/login` is Google-only now. Also fixed the same round: `createProject` was still redirecting to the old `/projects/[projectId]/modules` page (REQ-074, pre-rebrand) after creating a project — a brand-new signup's first project dumped them into an unbranded legacy screen; now redirects to `/dashboard`.

**Also confirmed working in the real world**: the user reset all app data via `truncate table companies cascade` (to test the fresh-signup path) and went through actual Google Sign-In → `/onboarding` → created a real company for real, proving Part C's onboarding branch works end-to-end outside of any test harness.

### 27. Team Members UI + invite claim flow (Part D, qa-agent-spec.md new section — new)
The schema and RLS for this (`company_members.invite_token`/`invite_status`/`invite_expires_at`, `admin_manage_members`/`admin_update_members` policies) shipped with Part A back in §26, but the actual page was never built — the user caught this directly ("icant see option add members, did you add it?"). Built now:

- **`app/team/page.tsx`** + **`TeamMembersClient.tsx`** — company-scoped (not project-scoped), reachable from a new "Team Members" entry in `Sidebar.tsx`'s bottom nav (`IconUsers` added to `icons.tsx`). Since `/team` is a standalone top-level route, it gets none of the app's existing layouts for free — the dashboard's `Sidebar` component is tightly coupled to per-project tab state that doesn't apply here, and the old legacy `/projects/[projectId]/layout.tsx` nav is exactly what §26 already flagged as the wrong thing to land users on. Gets its own light, branded header instead (HappyApp mark + "Back to Dashboard").
- **`components/team/AddTeamMemberModal.tsx`** — Name, Role, an "Admin access" checkbox; matches `SendToDevsModal.tsx`'s established convention exactly. `createTeamInvite` (`app/team/actions.ts`) generates the token, no email sent — the admin gets the link back and shares it themselves, per the original ask.
- **Expire/Renew** (`expireInvite`/`renewInvite`) only ever touch `invite_expires_at` — never `invite_token` — so "the link stays the same, only renews" holds exactly. "Expire now" sets it to `now()`; `components/team/RenewInviteModal.tsx` lets the admin pick a future date/time or "never expires".
- **`app/invite/[token]/page.tsx`** (public, admin client, `proxy.ts` already exempted `/invite/*` when Part C shipped) — shows "You've been invited to join `<Company>` as `<Role>`" + a **Continue with Google** button (`app/invite/[token]/JoinButton.tsx`) that calls `signInWithGoogle` with `?invite_token=` tacked onto the redirect, which `app/auth/callback/route.ts`'s existing invite branch (also shipped with Part C, just never had a UI in front of it until now) claims.
- **A real bug caught before shipping**: the client's optimistic "add member" update initially generated a fake local `crypto.randomUUID()` for the new row's `id`, which wouldn't match the real database row — Expire/Renew on a freshly-added member would have silently failed (wrong id) until a page refresh. Fixed by having `createTeamInvite` return the real inserted row's id.

**Verified directly against the real database** (not just `tsc`/`eslint`/`next build`, though those are all clean too) — since Google-only auth means a full Playwright browser login can no longer be scripted, verified via a temporary password-based test account (admin API) exercising the actual RLS policies with real REST calls: a non-admin's insert attempt correctly rejected (403), roster read correctly allowed for any active member, an admin's insert/update correctly succeeded, and the invite-claim update shape (same as the callback route's) correctly enforces the one-membership-per-company-per-user constraint. All test data deleted afterward.

### 28. Documents, Features/Suggestions, Notes, Personal Tasks, "For Vibe Coding" (qa-agent-spec.md new section — new, migration 0017 not yet applied)
A large batch, resolved via two clarifying questions before writing schema (both answered directly by the user): Documents/Features/Suggestions are **per-project** (scoped to whichever project is selected, like the existing board tabs); Notes/Personal Tasks are **company-wide and private to the signed-in member**, with an optional per-item project tag/filter dropdown rather than a hard scope. Features and Suggestions are two separate tabs in the UI, backed by one shared `feature_requests` table (`kind` column) since the shape is identical — avoids duplicating RLS/indexes/columns for what's otherwise one entity, consistent with how support tickets already reuse `board_issues`.

**Schema** (`0017_documents_features_notes_tasks.sql`): `documents` (admin-insert/delete-only, any active member can read), `feature_requests` (any active member can insert/update — open, collaborative, not admin-gated), `notes` and `personal_tasks` (RLS: `user_id = auth.uid()` only — the first tables in this app scoped to an individual member rather than shared company-wide via `is_staff()`).

**Navigation restructuring**: `lib/board/types.ts` gained `BoardView = TabKey | ExtraView` (`ExtraView` = features/suggestions/notes/personal_tasks/vibe_coding) and `VIEW_ORDER`/`VIEW_LABELS` covering both, so `Sidebar.tsx` and `TabNav.tsx` render the new destinations in the same nav as the existing 6 board tabs — the user called all of them "tabs" even though four of the five are backed by entirely different tables than `board_issues`. `DashboardClient.tsx` branches on `isTabKey(activeView)`: true renders the existing `IssueCard` list against `board_issues`; false renders one of the new dedicated panel components. Documents is the exception — described as "in the sidebar" rather than "a tab," so it's a bottom-nav link to `/projects/[projectId]/documents` instead, alongside Team Members/Links.

**TopBar's "New Issue" button is now "Add"** — a popup with Issue/Feature/Suggestion (`components/dashboard/AddFeatureModal.tsx` for the latter two). **`MoveToMenu.tsx`** (the dev-facing dropdown on every issue card) gained "Move to Feature"/"Move to Suggestion" — `convertIssueToFeatureRequest` (`app/dashboard/featuresActions.ts`) inserts the `feature_requests` row (with `source_issue_id` set, so the card shows where it came from) and moves the original issue to `closed` rather than deleting it, same audit-trail-preserving pattern as every other conversion in this app.

**"For Vibe Coding" tab** (`components/dashboard/VibeCodingPanel.tsx`), added in a follow-up message: pick an Issue (further filterable by workflow stage)/Feature/Suggestion, refine its description in a "Dev Description" field pre-filled from the original report, generate a text-only PDF (`jspdf`, dynamically imported inside the click handler so it never touches browser globals during SSR) to hand to an external AI coding tool. **Closes the loop with the existing `ai_fix` tab** (a second follow-up message): "Send to AI Fix" moves the issue there once an external AI attempt has been made; verifying it as *not* actually fixed just uses the *existing* comment/notes system on the issue (no new schema needed) — and picking that same issue again in Vibe Coding now fetches every `board_issue_comments` row for it and folds them into the Dev Description pre-fill, so a re-generated PDF carries the full "original report → AI fix note → why it wasn't fixed" trail instead of just the original text.

`tsc`/`eslint`/`next build` all clean. **Migration 0017 has not been applied yet** — none of this has been exercised against the real database.

### 29. Letting an external AI tool report back (REQ-155 extension, migration 0018 — new)
Follow-up to §28: the user asked whether Claude/Codex, after fixing an issue from a Vibe Coding PDF, could actually update HappyApp themselves. Answer was no (the PDF is a static, one-way text file — no credential, no endpoint) — so built the missing piece, deliberately keeping the PDF description-only per the original ask by putting the callback instructions in a **separate** copyable block next to it, not inside the PDF.

**`projects.api_token`** (migration 0018, `uuid default gen_random_uuid()` — every project gets one automatically, existing and new) scopes a new unauthenticated-by-session endpoint, **`POST /api/vibe-coding/issues/[issueId]`** (`proxy.ts` exempted, same "server calling in" reasoning as the Slack/WhatsApp webhooks) — deliberately narrow: it can only move the issue to `ai_fix` and leave a comment with the AI's summary, nothing else (can't close/delete, can't touch another project). `VibeCodingPanel.tsx` shows the exact `curl` command (real token + issue id filled in) once an issue is selected, for the dev to hand their AI tool directly — if the AI runs it after finishing, the issue lands in AI Fix with its own summary attached as a note automatically.

**AI Fix tab got the verify/fix/retry loop made explicit**: `IssueCard.tsx` shows a "Verify needed" badge on any `ai_fix`-tab issue, plus two one-click actions that were previously only reachable through the generic move dropdown — **Mark as Fixed** (→ `done`) and **Send back to Vibe Coding** (→ `pending`, so the dev adds a note explaining what's still wrong via the existing comment system, then re-picks the issue in Vibe Coding — REQ-155's existing comment-trail-folding already carries that note into the next PDF).

No token-rotation UI yet (the default-generated token can't currently be regenerated from the app if it needs invalidating) — worth adding if this sees real use.

`tsc`/`eslint`/`next build` all clean. **Migration 0018 has not been applied yet.**

### 30. Manual issue priority reordering + Details button + image lightbox (qa-agent-spec.md new section — new, migration 0019)
Three related IssueCard/detail-panel changes from real usage feedback:

- **Reordering**: `board_issues.sort_order` (migration 0019, backfilled from `created_at` so existing lists keep today's order until someone actually reorders) — up/down arrow buttons now sit where the card's thumbnail used to be, swapping the issue's `sort_order` with whichever neighbor is directly above/below it in the *currently visible* (filtered) list (`app/dashboard/actions.ts`'s `reorderIssues`, just two UPDATEs). `visibleIssues` now sorts by `sort_order desc` first, `created_at desc` as a tiebreaker.
- **Explicit "Details" button**: the whole card was previously one big click target (`onClick` on the outer div) opening the detail panel — removed in favor of a labeled "Details" button plus the title/message block itself still being clickable, since the card's left edge is now interactive (the reorder arrows) and a stray click there shouldn't accidentally open the panel.
- **Image lightbox in the detail panel**: the large `Thumbnail` at the top of `IssueDetailPanel.tsx` is now tappable when there's a real image, opening the same full-screen lightbox pattern already proven in `ChatWindow.tsx` (Escape key, backdrop click, close button) — previously it was a static, non-interactive image.

`tsc`/`eslint`/`next build` all clean. **Migration 0019 has not been applied yet.**

### 31. Vibe Coding / AI Fix polish round (bug fixes + numbering, no new migration)
A batch of smaller fixes and refinements caught through real usage, none requiring schema changes:

- **Fixed: broken images on live-Realtime-inserted issues.** `media_url` is a private Storage path only resolved to a signed URL by `app/dashboard/page.tsx`'s initial server load — `DashboardClient.tsx`'s Realtime `INSERT` handler was passing the raw path straight through instead, rendering a broken image on any issue that arrived live (Slack/WhatsApp/Team Report) rather than via a fresh page load. Now resolves the signed URL inside the Realtime handler too.
- **Fixed: checking multiple items in Vibe Coding only produced one issue in the generated PDF.** The type/status filter dropdowns were calling `setSelectedIds(new Set())` on every change, silently dropping earlier picks. Removed those resets; selections now persist across filter changes using the `allItems`/`items` split already described in §28.
- **Fixed: attached screenshots were missing from the generated PDF** even though they showed on the issue card — jsPDF needs actual image bytes (`getImageProperties`/`addImage` on a data URL), not a remote `src`. `VibeCodingPanel.tsx` now fetches each selected issue's signed image URL, converts it to a data URL (`fetch` → `blob` → `FileReader`), and embeds it under an "Attached screenshot" heading.
- **PDF layout**: multiple selected items no longer force a page break between them — they flow one after another separated by a horizontal rule, only breaking pages where content naturally runs out of room.
- **Vibe Coding picker now shows the attached screenshot inline** (when expanded) and is tappable to open the same full-screen lightbox pattern used elsewhere in the app, instead of only being visible after generating a PDF.
- **Stable "Issue 1 / Feature 1 / Suggestion 1…" numbering**, requested to match between the Vibe Coding PDF/picker and the AI Fix tab. Previously the PDF numbered only the currently-*checked* subset in selection order, so the same issue's number could change depending on what else was checked alongside it. Now `VibeCodingPanel.tsx` computes a stable `itemNumbers` map from each item's position in the full, unfiltered list (per kind), and both the picker rows and the PDF headings read from that map. `IssueCard.tsx` takes a `displayNumber` prop (`DashboardClient.tsx` passes each AI Fix card's 1-based position in the visible list) and renders "Issue N:" ahead of the title, but only on the AI Fix tab.
- **Issues already in AI Fix no longer appear in the Vibe Coding picker** — once an issue has been sent off and fixed (or claimed fixed), re-picking it in Vibe Coding made no sense until a human sends it back to Pending/In Progress from the AI Fix tab; it's now filtered out of `allItems` entirely (and "AI Fix" removed from the status-filter dropdown, since it would always show nothing).
- **PDF's "Instructions for AI" now requires the report-back callback to be run for every item, even when the AI couldn't find or reproduce the issue** — previously an AI tool that hit a dead end could just skip the callback silently, leaving no record. The summary text is where it explains what it found (or didn't).

`tsc`/`eslint`/`next build` all clean.

### 32. "Later On" kind (feature_requests, migration 0020 — new)
A third `feature_requests.kind` alongside `feature`/`suggestion` (§28), for items the dev wants to track but is deliberately deferring rather than acting on now — distinct from a feature idea or a suggestion. Reuses the exact same table/RLS/status machinery, no new schema besides widening the `kind` check constraint (`0020_feature_requests_later_on_kind.sql`).

Wired into every place Feature/Suggestion already appear, so nothing net-new had to be built:
- **TopBar's "Add" popover** — now Issue/Feature/Suggestion/Later On (`AddKind` widened in `TopBar.tsx`).
- **The dev-side "Move to…" menu on an issue card** (`MoveToMenu.tsx`) — gained "Move to Later On", alongside the existing "Move to Feature"/"Move to Suggestion".
- **A new sidebar tab**, "Later On" (`lib/board/types.ts`'s `ExtraView`/`EXTRA_VIEW_ORDER`/`VIEW_LABELS`, positioned right after Suggestions), rendered by the same `FeaturesPanel.tsx` component already shared by Features/Suggestions — just a third `kind` value. New `IconHourglass` (`icons.tsx`) for its sidebar icon.
- **Vibe Coding's type filter and numbering** — `VibeCodingPanel.tsx`'s `SourceType`/`KIND_LABEL`/`PickerItem.kind` all widened to include `later_on`, so a Later On item can be picked and exported into an AI PDF the same as a Feature/Suggestion, with its own stable "Later On N" numbering.

The three places that used to hardcode `kind === "feature" ? "Feature" : "Suggestion"`-style ternaries (`featuresActions.ts`, `DashboardClient.tsx`'s `handleSubmitFeature`/`handleConvert`) now read from one shared `FEATURE_REQUEST_KIND_LABELS` map (`lib/types/database.ts`) instead of a third hardcoded ternary branch each.

`tsc`/`eslint`/`next build` all clean. **Migration 0020 has not been applied yet.**

### 33. Project name embedded in shareable Report/Support links (no migration)
The "Internal Team" and "Customer Support" links (§14, `LinksCard.tsx`) were bare UUIDs (`/report/<uuid>`, `/support/<uuid>`) — with several projects each generating their own link, there was no way to tell which project a copied link belonged to just by looking at the URL. Caught directly by the user pointing at a real link and asking for the project name to be visible in it.

**`lib/board/projectSlug.ts`** (new) — `projectSlugPath(id, name)` builds a vanity route segment (`"<slugified-name>-<uuid>"`, e.g. `acme-app-6b351396-…`), `extractProjectId(routeParam)` pulls the real UUID back out via a trailing-UUID regex, ignoring whatever slug is in front. `LinksCard.tsx`'s two links (via `app/projects/[projectId]/links/page.tsx`) now generate the slugged form; `app/report/[projectId]/page.tsx` and `app/support/[projectId]/page.tsx` both call `extractProjectId()` on the route param before doing anything else with it, so every downstream lookup, bound server action, and client-component prop gets the clean UUID exactly as before — nothing past that first line changed.

**Old bare-UUID links (already shared, before this existed) keep working unchanged** — a bare UUID is itself a valid match for the trailing-UUID regex, so `extractProjectId` returns it as-is. Verified both forms resolve identically against the same real project in production.

`tsc`/`eslint`/`next build` all clean. No migration — display/routing only.

### 34. Whole-export "Note for AI" in Vibe Coding (no migration)
A free-text field that applies to the entire batch, not one item — for things like setup steps or "ignore the styling on #2 for now" that don't belong on any single item's Dev Description. `VibeCodingPanel.tsx` gained a `noteForAi` state + textarea, rendered between the filter bar and the item list; `handleGeneratePdf` writes it into the PDF as its own "Note from the team" section, right after the AI instructions and before every item's own section, only when non-empty. Session-only state (same as every other text field in this panel), not persisted server-side.

`tsc`/`eslint`/`next build` all clean. No migration — client-side only.

### 35. "Copy PDF Link" alongside "Download as PDF" in Vibe Coding (no migration)
The Generate PDF button now opens a small popover with two options, rather than always downloading a file — the user wanted to be able to paste a link directly into an AI chat tool instead of downloading and re-uploading the PDF by hand.

`VibeCodingPanel.tsx`'s PDF-building logic (previously all inline in one `handleGeneratePdf`) is now `buildPdfDocument()`, returning the finished `jsPDF` instance without saving it — shared by two thin wrappers: `handleDownloadPdf()` (the original `doc.save(...)` behavior) and `handleCopyPdfLink()`, which calls `doc.output("blob")`, uploads it to the existing private `whatsapp-media` Storage bucket under a random filename (`vibe-coding-<uuid>.pdf` — same bucket already used for report/support screenshots, covered by the same `is_staff()` insert/select policies from migration 0009, no new bucket or policy needed), creates a 7-day signed URL, and copies it to the clipboard. Transient "Link copied!"/"Failed to copy link" feedback shows next to the button rather than inside the popover, since the popover closes immediately on click but the upload+signing takes a moment.

**Found and left alone, not fixed**: `DashboardClient.tsx`'s unrelated `handleCopyLink`/"Copy Public PDF Link" (the per-issue link on `IssueCard`/`IssueDetailPanel`, §14-era) builds a URL against `https://qa-agent.internal/...` — a fake placeholder domain with no real route behind it, dating from the pre-§19 mock-data prototype. It was never wired up and still isn't; worth fixing the same way if that per-issue link is ever actually needed, but out of scope for this request.

`tsc`/`eslint`/`next build` all clean. **Not yet verified against a real signed-in session in a browser** — the upload/signing path reuses an already-proven bucket and policy, but nobody has actually clicked "Copy PDF Link" and confirmed the pasted link opens the real PDF yet.

### 36. "Copy as Text" added to the same menu (no migration)
Follow-up to §35: the user tried "Copy PDF Link" and pasted it into their AI tool, and got back a bare URL string, not usable content — a copied link pastes as a URL wherever it lands, nothing on the other end fetches it automatically. That's a fundamentally different need than §35's link (which is genuinely the right tool when a screenshot needs to be seen): this one is a plain-text copy of the exact same content, so pasting into a chat input lands the real, readable text immediately.

New third popover option, **`buildPdfText()`** — mirrors `buildPdfDocument()`'s structure and content (title, AI instructions, the team note if any, each item's kind/number/title/description, its report-back curl command) as one plain-text block instead of PDF layout calls, skipping the image fetch entirely since screenshots have no text form. `handleCopyPdfText()` calls `navigator.clipboard.writeText()` with it. The popover is now three options: Download as PDF, Copy as Text, Copy PDF Link.

`tsc`/`eslint`/`next build` all clean.

**§35 and §36 were both reverted immediately after** (same day) — what the user actually meant, once they clarified directly, was neither a link nor a text copy: they wanted the literal PDF *file* copied onto the OS clipboard the way `Ctrl+C` on a file in Finder/Explorer works, so any paste target attaches it as a real file with zero download step. That's not something a website can reliably do — there's no web API for a true OS-level file-clipboard write, and even the closest approximation (writing a `Blob` via the Clipboard API) only works in Chromium browsers and only if the paste target's own handler happens to accept a blob the same way it'd accept an OS file drop, which can't be promised for an arbitrary external AI tool. Asked the user whether to attempt that Chrome-only best-effort version anyway, or drop it — they chose to drop it and revert to the plain single-button "Generate PDF" (download only) that existed before §35, keeping every other change from that session (numbering, AI-Fix exclusion, the Later On kind, the Note for AI field). `VibeCodingPanel.tsx` is back to that exact pre-§35 shape.

**If "paste the actual file" ever comes up again**: the honest options are (a) the Chrome-only best-effort blob-clipboard write described above, with no guarantee the target app accepts it, or (b) accept that a download-then-manually-attach step is unavoidable for a real file, and instead make the *download* step as low-friction as possible (e.g. one click, sensible filename). Worth raising this trade-off explicitly before building either.

### 37. Personal Tasks: priority reordering, numbering, instant add (migration 0021)
Three related fixes to `PersonalTasksPanel.tsx` from direct usage feedback:

- **Priority reordering** — `personal_tasks.sort_order` (migration 0021, backfilled from `created_at`, same pair-swap mechanism as `board_issues.sort_order` from §30/migration 0019) with up/down arrow buttons per task. Unlike the issue board's descending convention, this one is **ascending** (lower = higher up the list) — a day's task list reads top-to-bottom "in the order added" by default with new tasks appended at the bottom, matching a simple to-do list rather than a reverse-chronological feed. `reorderPersonalTasks()` (`tasksActions.ts`) mirrors `reorderIssues()`.
- **1-based numbering** ("1.", "2.", "3.", …) next to each task, based on its position in the current day's list.
- **Fixed: adding a task felt slow.** Two things were going on: (1) `PersonalTasksPanel.tsx`'s `handleAdd` only appended the new task to local state *after* the server round trip resolved — now it appends a local placeholder immediately (a real optimistic insert, with a temp `"temp-<uuid>"` id, dimmed and its controls disabled until reconciled with the real row; rolled back on failure) — matching the pattern this app's memory already flags getting wrong once before (Team Members' fake-`crypto.randomUUID()` id bug); this time the temp id is clearly distinguishable and never sent to the server. (2) `createPersonalTask` was calling `supabase.auth.getUser()` a second time even though `getCurrentMember()` (called right before it) already resolves the same user — cut by adding `userId` to `CurrentMember` (`lib/company.ts`) so callers don't pay for a redundant round trip. `notesActions.ts`'s `createNote` has the identical redundant-call pattern but wasn't touched — not reported as slow, left as-is to keep this change scoped to what was asked.

`tsc`/`eslint`/`next build` all clean. **Migration 0021 has not been applied yet.**

---

## Pending / not built

- **Migrations `0017` through `0021` need to be applied** (in that order) — `0017`/`0018` appear to already be in use based on recent activity (Team Report/Feature submissions and the image-resolution bug both involved live `board_issues`/`feature_requests` rows), but `0019` (issue sort_order, §30), `0020` (the "Later On" kind, §32), and `0021` (personal_tasks sort_order, §37) are confirmed not yet applied. Walk through each surface once for real once applied, including both up/down reorder arrows (issues and personal tasks), the detail-panel image lightbox, and adding/converting an issue to a Later On item.
- **No way to regenerate a project's `api_token`** if it ever needs invalidating (§29) — would need a small admin-only action + button, likely on the Settings or Documents page.
- The CoachPro-inspired visual redesign (Part E) hasn't been started.
- **Full browser-level verification of the invite-claim flow is still open** — confirmed the underlying RLS/DB mechanics directly (see §27), but nobody has clicked through the actual `/invite/[token]` → Google consent → landing-in-the-right-company path in a real browser yet, since that requires a second real Google account. Worth doing once there's an actual second team member to invite for real.
- **The stale `https://qa-agent.internal/...` fake link in `handleCopyLink`/"Copy Public PDF Link"** (`DashboardClient.tsx`, per-issue "Copy Public PDF Link" button on `IssueCard`/`IssueDetailPanel`) — found while building §35 (since reverted, see §36's note), was never wired to a real route, dates from the pre-§19 mock-data prototype. Worth fixing the same way (or removing) if that per-issue link is ever actually used.

- **§25's ticketing feature has not been run against a real database yet** — migration `0013_support_tickets.sql` needs to be applied (Supabase SQL editor, same as every other migration this session), then walked through end-to-end for real before trusting it. See §25 above for the exact loop to test.

- **Dedup on module sync (REQ-010) is exact-name-only** — found during §12's verification (module count temporarily went 10 → 21 in the real "Sacrol" project before the 11 duplicates were confirmed test_case/issue-free and deleted). Not something the spec called for originally. Worth deciding whether to fuzzy-match names, or just accept it as an expected consequence of ever re-syncing modules against a different model than the one that created them.
- **Test case generation and Fix It (Qwen) are still only verified via raw `curl`, not through the app** — module sync (this session) proved the translation path works in general, but `test_case_generation`, `test_run`, and `fix_run` haven't each been individually exercised through a real UI-driven run yet. **Blocked on a real issue existing**: the "Sacrol" project currently has zero issues, so `fix_run` has nothing to run against. User is going to report/generate a real issue themselves (Report Issue form, or an automated test run) and triage it; once one exists and is `tag=bug`/`status=triaged`, trigger the Fix It batch on it to get the first real Qwen run through the app. Now that Sacrol's Appium bridge is live and `automation_target` points at it correctly, a real "QA It" run against Sacrol is also the natural way to get `test_run` its first real UI-driven verification.
- **The real Appium server + mobile bridge are running as background processes started manually this session** (`npx appium --port 4723`, `npm run bridge:appium`) — they will **not** survive a machine restart or this terminal session ending. Before testing Sacrol again after a restart: confirm the device is connected (`adb devices`), start Appium (`ANDROID_HOME=~/Library/Android/sdk JAVA_HOME=$(/usr/libexec/java_home) npx appium --port 4723`), then `npm run bridge:appium` (now auto-loads `.env.local`, including the app package/activity — see §8).
- **No automated test suite** (unit/integration tests for the app itself). This was built and verified interactively via real Claude/Supabase/browser runs, not via a CI-style test suite. Worth adding if this goes anywhere near production.
- **Per-project Appium capabilities** — device name / app package / app activity are global env vars right now, not stored per-project. Fine for Sacrol alone; breaks if a second mobile project needs different capabilities at the same time.
- **`localhost:3000` is no longer reliably this app.** User is running a separate project ("TaleemAI") that's now claiming port 3000; Next.js auto-bumps qa-agent's dev server to the next free port (currently **3001**) rather than failing, so it's easy to silently point a script/browser at the wrong app. Before any Playwright/`curl` verification against "the dev server," confirm which port qa-agent is actually on — e.g. `curl -s http://localhost:PORT/login | grep -o '<title>[^<]*</title>'` should say `QA Agent`, not something else — rather than assuming 3000.
- **iOS** — explicitly out of scope per spec (Section 10), but worth noting the mobile bridge is Android/UiAutomator2-only right now.
- **Send a real image through the WhatsApp inbox** — text is confirmed working end-to-end (§14); the image path (two-step Graph API media download + `whatsapp-media` upload + rendering on `/whatsapp`) is written and follows the same verified pattern as `evidence`, but hasn't been exercised by an actual incoming image yet.
- **The `ngrok` tunnel + Appium/mobile bridge are all background processes started manually in this session** — none of them will survive a machine restart or this terminal closing. The `ngrok` URL also changes on every restart, which means the webhook Callback URL saved in the Meta app dashboard would need to be updated to match before WhatsApp messages would flow again. Before relying on any of these after a break, confirm they're actually still running rather than assuming.
- **WhatsApp is currently only wired to Meta's free test number**, with testers explicitly allow-listed in the Meta dashboard — not yet the real "ImHealthee" business number the team would actually use day to day. That's Step 2 ("Production setup") in Meta's guided flow, deliberately deferred until the test-number path was confirmed working.
- Git: pushed to `https://github.com/wakassharif92/happyapp.git` (`main`) through the Slack integration + Support Chat/Team Report work (§19–23). **The HappyApp rebrand round (§24 — rename, Add/Edit Project, hidden automation field, unread indicator, image sharing, lightbox) is not yet committed** — confirm with the user before committing/pushing, per this repo's "only commit when asked" convention.
- **Slack integration (§19) has no real Slack App behind it yet** — every route, the encryption, the signature verification, and the dedup/rate-limit logic are all written and verified in isolation, but the actual OAuth consent screen, `conversations.list` channel listing, and a real incoming message have never been exercised, because that requires the user to create a Slack App and work through `docs/slack-setup.md` (needs a Slack workspace, and for local dev, a tunnel — same requirement as WhatsApp). Once a real App exists: connect a project, pick a channel, post a message with an image in it, confirm it lands in Pending with the image visible and a thread reply posted.
- **Migration `0005` (`team_reports.page_name`/`other_project_name`) status is still unconfirmed** — last known state (a few sessions back) was a Supabase-branch mismatch being worked through; never circled back to re-verify after §16/§19's branch-mismatch issues resurfaced the same failure mode. Re-check with the same `pg_publication_tables`-style direct query approach (§19) before trusting those two columns in production.

## Known non-obvious things (read before changing related code)

- **New Supabase tables are NOT automatically included in Realtime** — `postgres_changes` subscriptions only fire for tables explicitly added to the `supabase_realtime` publication (`alter publication supabase_realtime add table <name>;`). Forgetting this makes a Realtime subscription (e.g. `ActivityFeed.tsx`'s or `DashboardClient.tsx`'s pattern) silently receive nothing, with no error anywhere — found the hard way in §19, where the `alter publication` statement itself *also* silently failed to apply twice before a third attempt worked (same Supabase-branch-mismatch failure mode as migration `0005`). If a new Realtime-dependent table ever seems to do nothing, verify directly with `select tablename from pg_publication_tables where pubname = 'supabase_realtime';` rather than assuming the migration that was supposed to add it actually ran.
- **This project has repeatedly hit the same "SQL Editor pointed at a preview branch, not main" failure mode** (migration `0005`, then twice for the `board_issues` Realtime publication in §19) — a migration can appear to run successfully in the SQL Editor and still not affect the database the app actually talks to. When a migration's effect doesn't show up as expected, don't assume the SQL was wrong before confirming which branch it ran against.
- **A `postgres_changes` subscription with a server-side `filter:` param can report `"SUBSCRIBED"` and then silently deliver nothing** — found in §23 for `support_messages`/`support_conversations`, reproduced with the RLS policy simplified, `REPLICA IDENTITY FULL` set, a production build, and every other subscription on the page disabled — none of it was the cause. The only thing that fixed it: dropping `filter:` entirely and filtering client-side inside the callback instead, matching `DashboardClient.tsx`'s (the one other Realtime consumer in this app) pattern — it has never used `filter:`. Until this is understood better, **don't add a server-side `filter:` to a new `postgres_changes` subscription** — subscribe unfiltered and filter the payload in the handler, even though it means slightly more client-side work.
- **A previously-verified Realtime path can appear to break in Playwright while the app itself is fine** — mid-§23 diagnosis, `board_issues` Realtime (solid since §19) briefly seemed to stop delivering via Playwright, while a raw Node script for the same table kept working. This was a local Playwright/Chromium sandbox networking issue that session, not a real regression — a real-browser manual check from the user resolved it immediately. If a Realtime feature that was working suddenly seems broken only in automated browser tests, check a raw Node script first, and don't trust the automated result over a real browser without cross-checking.
- **Don't rely on the Realtime echo-back to show a sender their own just-sent message, especially if it carries an attachment** — found in §24: text-only support-chat messages' echo-back was reliable, but the sender's own echo for image-attached messages wasn't (root cause not fully isolated). Both `SupportChatClient.tsx` and `SupportInboxClient.tsx` now append the sender's own message directly from their `.insert().select("*").single()` response instead, with dedup-by-id guarding against the echo also arriving. Prefer this optimistic-append pattern for any new "sender sees their own message" UI in this app rather than trusting Realtime for it.

- **`lib/types/database.ts` must use `type X = {...}`, not `interface X {...}`.** With interfaces, `@supabase/postgrest-js`'s column-string inference (`.select("id")`) silently collapses to `never` for this exact dependency combination. Cost real debugging time once already — don't reintroduce interfaces there.
- **Triage/verify/fix tool calls that take an `issue_id`/similar id param should prefer `ctx.runId` over trusting the model's copy of the UUID**, per the bug found in Phase 6. The model is generally good at copying ids verbatim when given them, but don't rely on it alone when the orchestration code already knows the answer.
- **Bridge fire-and-forget runs** (`runTestSuite`, `triageIssue`, `runFix`) rely on the Next.js dev/prod server being one long-lived Node process (REQ-090's "local machine" model) — this breaks on serverless/edge deployment. Don't deploy this app to Vercel-style serverless without rearchitecting the background execution.
- **`.env.local` has real credentials** (Supabase project + Anthropic key) — it's gitignored, but don't accidentally read/log/paste its contents somewhere that leaves this machine.
- **The Supabase free-tier project can auto-pause after ~a week of no API activity.** Hit this directly (2026-08-24): every query started failing with `TypeError: fetch failed`, which first looked like the known transient DNS flake (see the coerceArrayField-era notes) — but a direct `curl` to the project URL returned a real, non-transient **Cloudflare 521 "Web server is down"**, not a DNS or timeout error. If Supabase calls start failing app-wide with no code change to explain it, check the project's status in the Supabase dashboard and resume it before debugging further — there's no API-key-based way to unpause it, only the dashboard.
- **`lib/agents/pricing.ts` hardcodes per-model $/token rates**, including a date-conditional branch for `claude-sonnet-5`'s introductory pricing (cutoff 2026-08-31) and unverified DeepSeek/Qwen entries flagged inline. If a provider changes pricing, or `lib/agents/modelRouting.ts` gets pointed at a model not in `STATIC_PRICING`, cost numbers will silently drift toward the Sonnet-5 fallback — there's no live pricing lookup. Update this file when a model or its price changes.
- **Never trust the shape of a `finishTools` result (`result.result`) without validating it.** Found in production use: for a large `submit_modules` payload (10 modules), Claude returned `modules` as a JSON-*encoded string* instead of a native array — and it was double-nested (`{"modules": "{\"modules\":[...]}"}`). `lib/agents/parseAgentResult.ts`'s `coerceArrayField()` unwraps this (string → parse → unwrap nested key → array), and every array-shaped agent result (`generateModules`, `generateTestCases`) goes through it now. Scalar fields (`status`, `resolved`, `summary`) got the same treatment as a defensive `typeof` check with a clear error message instead of a crash, though the string-encoding failure mode has only been *observed* for arrays so far. If a scalar field ever fails the same way, reuse the same unwrap-if-string pattern there too.
- **Assistant-turn message content in `runAgentLoop`'s `messages` array is *not* really `Anthropic.ContentBlock[]`** despite the type annotation — since model routing shipped (§12), it's `NormalizedContentBlock[]` (`lib/agents/providers/types.ts`, just `{type, text}` / `{type, id, name, input}`) cast in via `as unknown as`, because a real `Anthropic.ToolUseBlock`/`Usage` now carries provider-specific required fields (`caller`, `service_tier`, …) that don't exist for DeepSeek/Qwen. Anything that reads assistant-turn content from that array (there isn't much — it's mostly write-only history) needs to treat it as the normalized shape, not the full Anthropic SDK type.
- **`callModel()`'s fallback retry (`lib/agents/providers/adapter.ts`) is silent from the caller's point of view** — only `fix_run` has a configured fallback (Qwen → DeepSeek V4 Pro, per REQ-104), and a fallback-served turn only shows up as a `console.error` log plus a different `model` string in that turn's `agent_api_calls` row. There's no dashboard indicator that a run partially fell back — check server logs or the cost breakdown's `model` column if a fix run's provider mix looks surprising.

## Environment / running it

```
npm run dev                  # Next.js app
npm run bridge:playwright    # web automation bridge (needed for web projects)
npm run bridge:appium        # mobile automation bridge (needs Appium server + emulator running first)
```

Required in `.env.local` (see `.env.local.example`): `NEXT_PUBLIC_SUPABASE_URL`, `NEXT_PUBLIC_SUPABASE_ANON_KEY`, `SUPABASE_SERVICE_ROLE_KEY`, `ANTHROPIC_API_KEY`. Already configured on this machine.
